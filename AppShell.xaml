<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ProgramareRevizie.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ProgramareRevizie"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent Title="Welcome" 
                      ContentTemplate="{DataTemplate local:MainPage}" />

        <ShellContent Title="Programari"
                      ContentTemplate="{DataTemplate local:AdaugaProgramarePage}" />
        <ShellContent Title="Piese Auto" 
                      ContentTemplate="{DataTemplate local:AboutPage}" />
    </TabBar>

</Shell>

